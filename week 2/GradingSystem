public class GradingSystem {

    public static String classifyMark(int mark) {
        if (mark < 0 || mark > 100) {
            return "Invalid mark";
        } else if (mark >= 80) {
            return "Distinction";
        } else if (mark >= 70) {
            return "Merit";
        } else if (mark >= 50) {
            return "Pass";
        } else {
            return "Fail";
        }
    }

    public static void main(String[] args) {
        // Array of test values exactly as requested
        int[] testMarks = {-1, 0, 49, 50, 69, 70, 79, 80, 100, 101};
        
        System.out.println("--- Testing Grading System ---");
        
        // Loop through the test cases and display the returned classifications
        for (int mark : testMarks) {
            System.out.printf("Mark %3d : %s%n", mark, classifyMark(mark));
        }
    }
}
